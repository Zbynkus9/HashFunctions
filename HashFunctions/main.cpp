#include <iostream>
#include <chrono>

using namespace std;

template<typename K, typename V>
class HashNode {
public:
	K key;
	V value;
	HashNode(K k, V v) : key(k), value(v) {}
};

template<typename

int main() {
	return 0;
}